# 🏃‍♂️ 開発タスク (Current Sprint)

## 🚩 Sprint 2: ギミックの多様化 (〜今週末)
ユーザーの操作パターンを増やし、単調さを解消する。

### 🥅 Goal 7: 「解答欄のない問題」の実装 (Story Mode)
ストーリー進行や、ただ読むだけのページを作成する。
- [ ] **Step 1: データ定義**
    - [ ] `type: "story"` を追加
    - [ ] `riddles.json` にストーリー用のデータを追加（ID: 6）
- [ ] **Step 2: 画面実装**
    - [ ] `quiz.html` に `story` 用の表示ブロックを追加
    - [ ] 「回答」ボタンの代わりに「次へ」ボタン（次のIDへ遷移）を配置

### 🥅 Goal 8: 「プルダウン選択式」の実装 (Select Box)
入力ミスを防ぎ、選択肢から答えさせるクイズ形式を追加する。
- [ ] **Step 1: データ定義**
    - [ ] `type: "select"` を追加
    - [ ] `riddles.json` に選択肢 (`options`) を持つデータを追加（ID: 7）
- [ ] **Step 2: 画面実装**
    - [ ] `quiz.html` に `<select>` タグを使ったUIを追加
    - [ ] `options` リストをループして `<option>` を生成

### 🥅 Goal 9: 「ドラッグ&ドロップ」並べ替え問題の実装 (Sortable)
要素を並べ替えて正解の順序を作るパズルを追加する。
- [ ] **Step 1: データ定義**
    - [ ] `type: "sort"` を追加
    - [ ] `riddles.json` に並べ替え対象のデータ (`options`) を追加（ID: 8）
    - [ ] 正解データはカンマ区切り（例: "A,B,C"）で定義
- [ ] **Step 2: 画面実装 (HTML/CSS)**
    - [ ] `quiz.html` にドラッグ可能なリスト (`draggable="true"`) を作成
    - [ ] `style.css` にドラッグ中の見た目を追加
- [ ] **Step 3: JS実装**
    - [ ] `script.js` にドラッグ&ドロップで要素を入れ替えるロジックを追加
    - [ ] 並び順を文字列化してサーバーに送信する処理を実装

---
## ✅ 完了したタスク (Done)
（...前回の内容はそのまま...）