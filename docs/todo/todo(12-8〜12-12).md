# 🏃‍♂️ 開発タスク (Current Sprint)

## 🚩 Sprint 2: ギミックの多様化 (〜今週末)

ユーザーの操作パターンを増やし、単調さを解消する。

### 🥅 Goal 7: 「解答欄のない問題」の実装 (Story Mode)

ストーリー進行や、ただ読むだけのページを作成する。

- [x] **Step 1: データ定義**
  - [x] `type: "story"` を追加
  - [x] `riddles.json` にストーリー用のデータを追加（ID: 6）
- [x] **Step 2: 画面実装**
  - [x] `quiz.html` に `story` 用の表示ブロックを追加
  - [x] 「回答」ボタンの代わりに「次へ」ボタン（次の ID へ遷移）を配置

### 🥅 Goal 8: 「プルダウン選択式」の実装 (Select Box)

入力ミスを防ぎ、選択肢から答えさせるクイズ形式を追加する。

- [x] **Step 1: データ定義**
  - [x] `type: "select"` を追加
  - [x] `riddles.json` に選択肢 (`options`) を持つデータを追加（ID: 7）
- [x] **Step 2: 画面実装**
  - [x] `quiz.html` に `<select>` タグを使った UI を追加
  - [x] `options` リストをループして `<option>` を生成

### 🥅 Goal 9: 「ドラッグ&ドロップ」並べ替え問題の実装 (Sortable)

要素を並べ替えて正解の順序を作るパズルを追加する。

- [x] **Step 1: データ定義**
  - [x] `type: "sort"` を追加
  - [x] `riddles.json` に並べ替え対象のデータ (`options`) を追加（ID: 8）
  - [x] 正解データはカンマ区切り（例: "A,B,C"）で定義
- [x] **Step 2: 画面実装 (HTML/CSS)**
  - [x] `quiz.html` にドラッグ可能なリスト (`draggable="true"`) を作成
  - [x] `style.css` にドラッグ中の見た目を追加
- [x] **Step 3: JS 実装**
  - [x] `script.js` にドラッグ&ドロップで要素を入れ替えるロジックを追加
  - [x] 並び順を文字列化してサーバーに送信する処理を実装

### 🥅 Goal 10: ID:10「正規表現」問題の実装 (Text Type)

アルファベットと記号を使った正規表現クイズを追加する。

- [x] **Step 1: データ定義**
  - [x] `riddles.json` に ID:10 を追加（正規表現 `^[Tt]a[ck]oyak[iy]$`）
  - [x] 動作確認（正解: "takoyaki"）

### 🥅 Goal 11: 「ロゴ」問題の実装 (Image-Map Type)

エンジニアに馴染みのあるロゴを使った探索問題を追加する。

- [x] **Step 1: アイデア・素材**
  - [x] ロゴの選定（言語・ツールなど）
  - [x] 画像ファイルの準備 (`/static/images/` 配下)
- [x] **Step 2: データ定義**
  - [x] `riddles.json` に ID:11 を追加 (`type: "image-map"`)
  - [x] クリック座標エリア (`coords`) の特定と設定

### 🔧 Goal 12: 開発環境の連携 (Jira & GitHub)

タスク管理とコード管理を紐付けて、開発フローを効率化する。

- [x] **Step 1: 連携設定**
  - [x] Jira プロジェクトと GitHub リポジトリの接続設定
  - [x] 動作確認（コミットログが Jira に反映されるか）
